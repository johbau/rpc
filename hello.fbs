namespace Hello;

// Define basic types
typedef String string;

// Define our request message
table HelloRequest {
    // No fields needed for this simple request
}

// Define our response message and mark it as the root type
table HelloResponse {
    result: [string];
}

root_type HelloResponse;
